#include "holberton.h"

/**
*append_text_to_file - creates a file
*@filename: var
*@text_content: var
*Return: 1 OR -1
*/
int append_text_to_file(const char *filename, char *text_content)
{
	if (!filename)
		return (-1);
	if (!text_content)
		return (-1);
	return (0);
}
